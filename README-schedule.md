# mu-sched
`mu-sched` implements: a fast and lithe run-to-completion (single threaded) scheduler; deferred tasks; and safe, efficient linkage from interrupt to foreground levels.

TODO:
* pass data structures into sched_init() rather than compile-time constants.
* provide calls to measure high-water mark(s)
